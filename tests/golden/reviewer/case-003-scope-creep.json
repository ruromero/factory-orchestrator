{
  "name": "reviewer-flags-scope-creep",
  "phase": "reviewer",
  "inputs": {
    "issue_title": "Fix typo in error message",
    "diff": "--- a/config/config.go\n+++ b/config/config.go\n@@ -10,7 +10,30 @@\n func ParseConfig(path string) (*Config, error) {\n \tdata, err := os.ReadFile(path)\n \tif err != nil {\n-\t\treturn nil, fmt.Errorf(\"rading config: %w\", err)\n+\t\treturn nil, fmt.Errorf(\"reading config: %w\", err)\n+\t}\n+\n+\t// Added: config validation\n+\tif err := validateConfig(&cfg); err != nil {\n+\t\treturn nil, fmt.Errorf(\"validating config: %w\", err)\n+\t}\n+\n+\t// Added: config migration from v1 to v2\n+\tif cfg.Version == 1 {\n+\t\tcfg = migrateV1toV2(cfg)\n \t}",
    "plan": "Fix typo: 'rading' -> 'reading' in error message."
  },
  "assertions": [
    {"type": "output_contains", "value": "scope"},
    {"type": "output_not_contains", "value": "approve"},
    {"type": "severity_present", "value": "MEDIUM"}
  ],
  "pass_threshold": "7/10"
}
